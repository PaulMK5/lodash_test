'use strict';

const _ = require('lodash');

console.log(_.random(1, 10));

/* const fs = require('fs').promises;

fs.readFile('./text1.txt', 'utf-8').then(content => {
  const newContent = `old content: ${content}`;
});

fs.appendFile('./text1.txt', 'new new new', 'UTF-8'); */

// console.log(__filename);
// console.log(__dirname);

/* const { MyMath } = require('./MyMath.js');
const {
  utils: { func, func2 }
} = require('./Utils');

console.log(MyMath.sum(2, 2));
console.log(MyMath.multy(3, 2)); */
